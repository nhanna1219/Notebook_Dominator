<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/index.css" />
    <title>Role</title>
  </head>

  <body>
    <h1>Get All ROle</h1>

    <% if (typeof deleted !=='undefined' ) { %> <%if (deleted=='true' ) { %>
    <div style="color: green">Deleted Successfully</div>
    <% } %>
    <br />
    <% } %>

    <form action="/role" method="get">
      <input type="text" name="roleName" placeholder="Enter role name" />
      <button type="submit">Search</button>
    </form>
    <br />
    <a href="/role/create">insert</a>
    <br />

    <table border="1" cellpadding="0" cellspacing="0">
      <tr>
        <th>ID</th>
        <th>tên role</th>
        <th>Active</th>
        <th>Action</th>
      </tr>
      <% roles.forEach((item)=> { %>
      <tr>
        <td><%= item.id %></td>
        <td><%= item.name %></td>
        <td><%= item.is_active %></td>
        <td>
          <a href="/role/edit/<%= item.id %>">Edit</a> <br />
          <a href="/role/delete/<%= item.id %>">Delete</a>
        </td>
      </tr>
      <% }) %>
    </table>
    <table border="1" cellpadding="0" cellspacing="0">
      <tr>
        <th>ID</th>
        <th>user name</th>
        <th>First name</th>
        <th>Last name</th>
        <th>password</th>
        <th>Role</th>
      </tr>
      <% users.forEach((item)=>{ %>
      <tr>
        <td><%= item.id %></td>
        <td><%= item.username %></td>
        <td><%= item.first_name %></td>
        <td><%= item.last_name %></td>
        <td><%= item.password %></td>
        <td><%= item.role_name %></td>
      </tr>
      <%})%>
    </table>
    <br />
    <a href="/">back</a>
    <br />
  </body>
</html>
